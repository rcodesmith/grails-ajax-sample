import rps.Category
class BootStrap {

    def init = { servletContext ->

          /*  new Category(
                    name: "Music"
            ).save(flush:true)

            new Category(
                    name: "Concert"
            ).save(flush:true)

            new Category(
                    name: "Arts"
            ).save(flush:true)

            new Category(
                    name: "Music"
            ).save(flush:true)

            new Category(
                    name: "Festival"
            ).save(flush:true)

            new Category(
                    name: "Films"
            ).save(flush:true)

            new Category(
                    name: "Kids"
            ).save(flush:true)

            new Category(
                    name: "Nightlife"
            ).save(flush:true)

            new Category(
                    name: "Exhibitions"
            ).save(flush:true)

            new Category(
                    name: "Poetry"
            ).save(flush:true)

            new Category(
                    name: "Sports"
            ).save(flush:true)

            new Category(
                    name: "Performances"
            ).save(flush:true)

            new Category(
                    name: "Politics"
            ).save(flush:true)*/
    }
    def destroy = {
    }
}
